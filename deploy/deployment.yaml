apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: xtyu-tekton-tutorial
  namespace: test
spec:
  replicas: 1
  template:
    metadata:
      name: xtyu-tekton-tutorial
    spec:
      containers:
      - name: xtyu-tekton-tutorial
        image: __IMAGE__
